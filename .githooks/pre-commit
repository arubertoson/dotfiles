#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
TMP="$(mktemp -d)"
trap 'rm -rf "$TMP"' EXIT

export PATH="$(mise exec -C "$ROOT" -- printenv PATH)"
git -C "$ROOT" checkout-index --all --prefix="$TMP/"

cd "$TMP"
just format-check shell-syntax
