#!/bin/zsh

##
# Named directories
#
# Set these early, because it affects how dirs are displayed and printed.
hash -d dev=$XDG_DEV_HOME
hash -d zsh=$XDG_CONFIG_HOME/zsh
hash -d lbin=$XDG_BIN_HOME
hash -d znap=$XDG_CACHE_HOME/znap
hash -d notes=~/notes
hash -d config=~/.config
