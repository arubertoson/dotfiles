prefer-no-csd
screenshot-path null

debug {
    // Noctalia notification actions and window activation lack a valid serial.
    honor-xdg-activation-with-invalid-serial
}

hotkey-overlay {
    skip-at-startup
}
